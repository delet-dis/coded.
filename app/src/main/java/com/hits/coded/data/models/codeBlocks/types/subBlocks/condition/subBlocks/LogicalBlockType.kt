package com.hits.coded.data.models.codeBlocks.types.subBlocks.condition.subBlocks

import com.hits.coded.R

enum class LogicalBlockType(val resourceId: Int) {
    AND(R.string.and),
    OR(R.string.or),
    NOT(R.string.not),
}